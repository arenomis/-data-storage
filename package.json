{
  "name": "data-storage-spa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "serve": "live-server --port=5173",
    "start": "npm run build && npm run serve"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "typescript": "^5.0.0"
  }
}
